#include <stdio.h>
int main(){
    //initializing methods-
    //int main[]={96,97,98};//the size is specified by counting the values
    //printf("the values present in the array is %d,%d,%d\n",main[0],main[1],main[2]);
    //int main[3]={40,56,78};//specificing the size of array beforehand
    //printf("the values present in the array is %d,%d,%d\n",main[0],main[1],main[2]);
    //storing values in a array-//
    int main[3];
    scanf("%d",&main[0]);
    scanf("%d",&main[1]);
    scanf("%d",&main[2]);
    printf("the values present in the array is %d,%d,%d\n",main[0],main[1],main[2]);
}